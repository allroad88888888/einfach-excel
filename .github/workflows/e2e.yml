name: Solid e2e (paused)

# `solid-excel` 源码仅保留考古；该工作流故意没有自动触发，也没有可执行 job。
# 保留文件是为了让历史方案中的链接仍有明确落点。
on:
  workflow_dispatch:

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "solid-excel e2e is paused"
