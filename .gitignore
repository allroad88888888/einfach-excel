.temp
.swc
node_modules

# builds
build
dist
es
esm
cjs
@types

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

coverage
.idea
.claude/
.playwright-mcp/
.tmp
.rush
*.tsbuildinfo
packages/vali
.npmrc

# Rust
target/

# wasm-pack output for excel/solid-excel demo. Generated inside excel/solid-excel
# by `npm run build:wasm` (lite, no REGEX*) and `npm run build:wasm:full`
# (full, with REGEX*). Not committed — every checkout regenerates them.
excel/solid-excel/wasm-pkg/
excel/solid-excel/wasm-pkg-full/

# Playwright
test-results/
playwright-report/
playwright/.cache/

# ad-hoc sanity screenshots dropped at repo root (e.g. w5-*.png, w7-*.png)
/*.png

# CodeGraph local index (machine-local, rebuilt by `codegraph sync`)
.codegraph/
.codex-artifacts/

# TypeDoc is rebuilt by excel/excel-site's build script.
excel/excel-site/public/api-reference/
excel/excel-site/.astro/

# agent-harness conversation archives (never commit)
.agent-archive/
