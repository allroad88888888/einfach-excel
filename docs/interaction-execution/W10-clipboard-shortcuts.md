# W10：剪贴板快捷键契约

> 本波关闭“过期测试描述”与真实键盘合同之间的断层。Paste Special 已有 `@einfach`
> 命令意图、能力门控和对话框宿主；本波只验证用户实际按键，避免为已存在功能复制状态。

| 来源 Issue                            | 唯一模型                           | 状态                | 独占范围                    | 前置            | 交付                                                                              |
| ------------------------------------- | ---------------------------------- | ------------------- | --------------------------- | --------------- | --------------------------------------------------------------------------------- |
| UI-542 Paste Special 快捷键浏览器闭环 | `model-542-paste-special-shortcut` | 已完成（`12dd4c1`） | 剪贴板 E2E 审计与本执行账本 | UI-503、UI-519d | 真实 TS/WASM Worker 中，内部复制后 Ctrl/⌘+Alt+V 打开对应的 Paste Special 对话框。 |

## UI-542 执行树

```text
Paste Special 键盘合同
├── A. 现状复核（完成）
│   ├── Core 将 Ctrl/⌘+Alt+V 映射为 clipboard.pasteSpecial
│   ├── Grid 在 capability、source 与 payload 齐备时打开既有 Atom 会话
│   └── WASM 与 TS Worker 均已挂载真实 SpreadsheetPasteSpecialDialog
├── B. 过期断言清理（完成）
│   └── 移除把 Ctrl/⌘+Shift+V 误写为产品合同的跳过用例
├── C. 浏览器闭环（完成）
│   └── 在 WASM 与 TS Worker 经内部复制后触发 Ctrl/⌘+Alt+V，并断言对应对话框可见
└── D. 交付（完成；`12dd4c1`）
    └── 根节点已按 UI-542 单独提交
```

## 边界

- `@einfach` Atom 继续持有键盘意图、粘贴能力、剪贴板来源和对话框会话；本波不新增
  框架局部产品状态或第二套快捷键映射。
- `Alt+V` 是项目既有 Paste Special 组合键的一部分。`Shift+V` 仍属于普通粘贴路径，
  不能因为历史跳过测试而改变产品合同。
- 浏览器测试只通过真实 Worker Grid 的内部复制建立剪贴板上下文；不直接写入 Atom 或后台端口。
- `12dd4c1` 已在根节点复核后按 UI-542 单独提交；后续同类回归仍须独立复核。
