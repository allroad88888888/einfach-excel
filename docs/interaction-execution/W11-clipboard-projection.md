# W11：剪贴板投影可见性

> 本波关闭一个过期的浏览器跳过用例：通过现有名称框把目标列带入可见投影，补齐真实 Worker
> 后端的大范围粘贴回归证据；不新增滚动或状态机制。

| 来源 Issue                  | 唯一模型                          | 状态              | 独占范围                         | 前置   | 交付                                                                                                         |
| --------------------------- | --------------------------------- | ----------------- | -------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------ |
| UI-543 大范围粘贴浏览器闭环 | `model-543-clipboard-large-range` | 已完成（2712522） | 剪贴板 E2E、案例账本与本执行账本 | UI-542 | 在 TS/WASM Worker 后端以可见编辑写入 B2:E8 四角，复制 B2:E8、粘贴至 G2，再以名称框定位目标角点并验证显示值。 |

## UI-543 执行树

```text
大范围粘贴投影合同
├── A. 现状复核（完成）
│   ├── TS/WASM Worker demo 的普通粘贴已通过 keyboard intent 与 Clipboard API 建立内部来源
│   └── 名称框导航会驱动目标格进入当前 Grid 投影
├── B. 过期跳过清理（完成）
│   └── 从宽泛 audit spec 移除“J 列不在 DOM”的历史跳过用例
├── C. 浏览器闭环（完成）
│   └── 在 wasm/ts Worker 表面复制 B2:E8、粘贴 G2，再可见地核对 G2:J8 四角
└── D. 交付（完成）
    └── 根节点已复核双后端结果、文件行数和独占 diff，并以 2712522 单独提交
```

## 边界

- 选区、剪贴板来源和投影加载继续由已有 `@einfach` Atom/命令边界持有；本波不增加组件局部
  产品状态，也不直接读写 Atom 或 Worker 端口。
- 名称框是正式可见的用户导航入口；测试以它切换到 J 列目标角点，而非以 DOM 注入或后台断言
  替代用户可见结果。
- 若真实 TS/WASM 路径无法投影或呈现值，保留失败并把问题回报为产品缺陷，不能把断言降为
  history、状态栏或“非 Ready”。
