---
title: 滚到哪，加载哪
summary: 把远端表格看成可以独立加载、失败、失效和重载的地址区域。
---

## 试试这个

1. 移动视口，请求另一块区域。
2. 重试可见区中失败的区域。
3. 重载一个过期区域，不重置整个工作簿。

## 它是怎么做到的

UI 通过 backend port 描述可见区域。生产中的远端 backend 可以暴露加载和失败状态，同时保留 worker 演示使用的同一份网格契约。
