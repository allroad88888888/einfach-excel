---
title: 只画看得见的
summary: 在 100,000 行订单表里滚动时，浏览器只向 worker 请求一个很小的可见投影窗口。
---

## 试试这个

1. 连续滚动几屏，网格仍应保持可交互。
2. 在名称框输入 F45000，直接跳到深处。
3. 编辑一个值，工作簿仍留在 worker 中。

## 它是怎么做到的

UI 把视口指标发送给 backend port。Rust/WASM 工作簿和公式留在 Web Worker，浏览器只接收可见行和少量预取行。
