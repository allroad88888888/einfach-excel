---
import DocsPage from '../../../components/DocsPage.astro'
import { sourceUrl } from '../../../data/source-projection'
import { sitePath } from '../../../data/site-path'
---

<DocsPage
  locale="zh"
  title="React 工作簿产品"
  eyebrow="私有 VITE 产品"
  summary="运行仓库中的完整 React 工作簿，并使用其 Rust/WASM worker。"
  alternateLocaleHref={sitePath('docs/react/')}
>
  <div class="docs-guide">
    <section class="docs-guide-callout docs-guide-callout-warning" aria-label="React 产品状态">
      <p class="docs-guide-label">状态：仓库内产品</p>
      <p>
        <code>excel/react-excel</code> 是私有 Vite 应用，不是可安装的集成库。请从本仓运行，
        并把它的源码视为一个完整产品，而不是面向宿主的 API。
      </p>
    </section>

    <h2>1. 运行工作簿</h2>
    <pre><code>{`git clone https://github.com/allroad88888888/einfach-excel.git
cd einfach-excel
pnpm install
pnpm --filter @einfach/react-excel dev`}</code></pre>
    <p>开发服务器监听 <code>http://127.0.0.1:5183</code>。</p>

    <h2>2. 理解 Rust-only 运行时</h2>
    <p>
      应用从 <code>@einfach/solid-excel/vnext-worker-runtime?worker</code> 创建 backend。
      工作簿读取和确认后的编辑会跨过 worker 边界进入 Rust/WASM；没有 TypeScript 引擎、静态 backend
      或运行时 fallback。
    </p>

    <h2>3. 使用当前产品能力</h2>
    <div class="docs-guide-terms">
      <article><strong>有界投影</strong><span>浏览 1,000 条销售订单，React 只挂载 32 行窗口。</span></article>
      <article><strong>选择</strong><span>选择单格或拖出范围，包括滚动到最后一条记录之后。</span></article>
      <article><strong>单元格编辑</strong><span>双击或按 Enter 开始编辑，再用 Enter 或失焦经 Rust 提交。</span></article>
    </div>
    <p>按 Escape 可取消编辑且不写入。</p>

    <h2>4. 验证产品</h2>
    <pre><code>{`pnpm --filter @einfach/react-excel typecheck
pnpm --filter @einfach/react-excel build
pnpm --filter @einfach/react-excel test`}</code></pre>

    <h2>5. 阅读产品源码</h2>
    <div class="docs-guide-next">
      <a href={sourceUrl('excel/react-excel/README.md')}><strong>阅读产品边界</strong><span>查看当前能力、目录和命令。→</span></a>
      <a href={sourceUrl('excel/react-excel/src/app/App.tsx')}><strong>阅读应用启动</strong><span>跟踪 Rust backend ready 到工作簿 runtime。→</span></a>
      <a href={sourceUrl('excel/react-excel/src/workbook/backend/rust-backend.ts')}><strong>阅读 worker 边界</strong><span>查看种子导入和 Rust/WASM backend 创建。→</span></a>
    </div>

    <h2>当前边界</h2>
    <p>
      ribbon、公式栏、sheet tab 和缩放表面目前只负责展示。剪贴板、历史、格式、sheet 命令及其他
      工作簿命令尚未接线；这个私有产品也不承诺 npm 发布或浏览器支持范围。
    </p>
  </div>
</DocsPage>
