---
import DocsPage from '../../../components/DocsPage.astro'
import { sourceUrl } from '../../../data/source-projection'
import { sitePath } from '../../../data/site-path'
---

<DocsPage
  locale="zh"
  title="React 适配器源码参考"
  eyebrow="预发布框架参考"
  summary="在 React 中渲染调用方持有的投影，同时保持工作簿边界清晰。"
  alternateLocaleHref={sitePath('docs/react/')}
>
  <div class="docs-guide">
    <section class="docs-guide-callout docs-guide-callout-warning" aria-label="React 适配器状态">
      <p class="docs-guide-label">状态：仅仓库源码</p>
      <p>
        <code>@einfach/react-excel</code> 是版本为 <code>0.0.0</code> 的私有 workspace 包。它尚未发布 npm，
        不提供默认 Worker 或 WASM 后端，也不是开箱即用的电子表格应用。现在不要在产品文档中写 npm 安装命令。
      </p>
    </section>

    <h2>1. 安装仓库 workspace</h2>
    <p>React 适配器当前从这个仓库中消费，因此先安装一次 workspace 依赖，再运行它的 fixture。</p>
    <pre><code>{`git clone https://github.com/allroad88888888/einfach-excel.git
cd einfach-excel
pnpm install`}</code></pre>

    <h2>2. 运行已验证的 React fixture</h2>
    <pre><code>{`NO_PROXY=localhost,127.0.0.1 \
pnpm --filter @einfach/react-excel run e2e`}</code></pre>
    <p>
      此命令会启动仓库内 Vite fixture、打开 Chromium、从一个受控单元格拖动到另一个单元格，并验证投影后的
      选区。成功时会报告一个通过的 React adapter 测试。
    </p>

    <h2>3. 了解当前可用边界</h2>
    <div class="docs-guide-terms">
      <article><strong>受控网格</strong><span>渲染由你的应用已经持有的可见单元格投影。</span></article>
      <article><strong>Provider hooks</strong><span>将选区、导航、编辑、剪贴板与历史桥接到 UI-core atoms。</span></article>
      <article><strong>显式后端</strong><span>由你提供 <code>SpreadsheetBackend</code>，不会暗中附带数据或计算运行时。</span></article>
    </div>

    <h2>4. 组合一个受控投影</h2>
    <p>这是一种仓库源码模式，不是包安装配方。React 宿主拥有 backend、cells 与请求窗口。</p>
    <pre><code>{`import type {
  CellRange,
  DisplayCell,
  SpreadsheetBackend,
} from '@einfach/spreadsheet-ui-core'
import {
  SpreadsheetGridView,
  SpreadsheetUiProvider,
} from '@einfach/react-excel'

type ProjectionProps = {
  backend: SpreadsheetBackend
  cells: readonly DisplayCell[]
  window: CellRange
}

export function ReactSpreadsheet({ backend, cells, window }: ProjectionProps) {
  return (
    <SpreadsheetUiProvider backend={backend}>
      <SpreadsheetGridView cells={cells} window={window} />
    </SpreadsheetUiProvider>
  )
}`}</code></pre>

    <h2>5. 这些职责仍由宿主承担</h2>
    <ol class="docs-guide-steps">
      <li>一个工作簿生命周期内保持 backend 实例稳定；替换它会有意创建新的 UI core。</li>
      <li>获取或推导有界投影，再将 <code>cells</code> 与 <code>window</code> 传给受控视图。</li>
      <li>增加交互时使用最近 provider 的 hooks；不要再建一份 React 状态来保存电子表格产品状态。</li>
    </ol>

    <h2>6. 查看交互参考实现</h2>
    <div class="docs-guide-next">
      <a href={sitePath('zh/demos/react-controlled-projection/')}><strong>运行 React 投影演示</strong><span>在固定 4×4 网格上拖动，并查看当前选择范围。→</span></a>
      <a href={sourceUrl('excel/excel-site/src/islands/ReactAdapterDemoIsland.tsx')}><strong>阅读精确的宿主源码</strong><span>它提供确定性 backend、store、投影与指针选择。→</span></a>
      <a href={sourceUrl('excel/react-excel/README.md')}><strong>阅读包边界</strong><span>查看已有基础能力与刻意未包含的部分。→</span></a>
    </div>

    <h2>还没有准备好的部分</h2>
    <p>
      目前没有已发布的 React 版本、默认 Worker 工厂、WASM 打包、浏览器兼容承诺或完整工作台外壳。若现在要正式接入，
      请使用受支持的 <a href={sitePath('zh/docs/getting-started/')}>Solid 路线</a>，或在 React 适配器成熟前实现
      <a href={sitePath('zh/docs/backend-port/')}>backend port</a>。
    </p>
  </div>
</DocsPage>
