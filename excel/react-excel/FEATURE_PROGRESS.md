# React Excel 主线执行记录

目标：按每批三个相关功能持续交付完整 React Excel，生产链路仅使用 UI Core atom + Rust/WASM。

## 范围与完成口径

- 原 React 三项启动任务已完成，不能据此宣布完整产品完成。
- 完整功能范围暂以[历史在线 Excel 清单](../solid-excel/docs/archive/online-excel-parity/README.md)
  的 41 项为核对索引；只引用功能范围，不继承 Solid 的实现状态或架构。
- 用户如指定另一份计划，以指定计划为准。Solid、Vue、介绍站当前不作为本目标的交付对象。
- 每项必须有 React 入口、真实 Rust 行为、可复现测试，才能记为完成；部分入口或只有核心单测
  一律记为部分完成。外部数据、身份、持久化服务不能用本地假数据替代验收。
- 下面按原编号合并展示相关项，不删减原清单；原计划明确延后的外部连接保留为延后状态。

## 全范围核对

| 原编号 | 能力 | React 当前状态 / 剩余工作 |
| --- | --- | --- |
| 01 | 工作表管理 | 初始工作表可加载；创建、切换、重命名、删除、排序待接入 |
| 02 | 行列结构、清空 | 清除内容/格式/全部已接入；行列插入、删除待接入 |
| 03 | 行高、列宽、隐藏 | 大字体可撑高所在行；手动尺寸、隐藏及恢复待接入 |
| 04 | 合并与拆分 | 待接入 |
| 05 | 冻结窗格 | 待接入 |
| 06 | 选区、定位 | 单区、行列整表、键盘、名称框已有；非连续多区、完整定位 UI 待补 |
| 07 | 分组与大纲 | 待接入 |
| 08 | 直接编辑 | 编辑、提交、取消、连续编辑、原始数值编辑、Alt+Enter 换行和输入法组合键保护已有；完整类型输入、真实系统输入法跨浏览器交互仍需验收 |
| 09 | 公式栏与引用 | 公式栏可编辑；引用拾取、补全待接入 |
| 10–11 | 剪贴板、选择性粘贴 | 系统复制/同表剪切/矩形粘贴、仅值、仅格式、选区平铺、转置、跳过空白、值及格式已接入 Rust；跨表剪切、部分公式范围移动、多区域、Copy As、仅公式、运算、批注及列宽等选项待补 |
| 12 | 自动填充与序列 | 待接入 |
| 13 | 分列 | 待接入 |
| 14 | 查找替换 | 待接入 |
| 15、36 | 撤销重做及历史 | 待接入 Rust 权威历史 |
| 16 | 富值及特殊类型 | 基础值渲染已有；特殊类型入口与编辑待补 |
| 17 | 常用格式工具 | 常用字体、颜色、对齐等已有；完整选择控件及命令待补 |
| 18–19 | 单元格格式、数字格式 | 三类数字格式及精度已有；完整格式会话与类别待补 |
| 20 | 格式刷 | 待接入 |
| 21–22 | 条件格式、数据验证 | 待接入 Rust 权威规则与 React UI |
| 23 | 边框、旋转及格式投影 | 基础边框、旋转已有；完整选择与投影语义待补 |
| 24–26 | 公式解析、函数目录、自定义公式 | 引擎已有部分计算能力；逐项核对原清单并接入 React 产品入口 |
| 27–30 | 筛选、排序及数据清理 | 待接入 |
| 31 | 状态栏摘要 | 选区格数已有；数值计数、求和、平均等待接入 |
| 32 | Excel Table | 待接入 |
| 33 | 外部数据连接 | 原计划延后；不以本地 fixture 宣称完成 |
| 34–35 | 批注、备注及协作 | 待接入；身份、通知及持久服务需要明确的真实契约 |
| 37–39 | 更改记录、版本、个人视图 | 待接入；本地 undo 不能替代版本服务 |
| 40 | 保护与解锁 | 核心有检查；Rust 权威写入与 React UI 待核对接入 |
| 41 | 加载、投影、恢复 | Rust 初始化、可见窗口和过期结果防护已有；完整恢复场景待验收 |

## 批次账本

| 批次 | 功能 | 证据 / 状态 |
| --- | --- | --- |
| 既有基线 | 编辑、导航、选择、常用格式 | `736a2109` 及其前序提交；相关 `test/`、`e2e/` |
| 数字精度 | 增加小数位、减少小数位、恢复常规 | `736a2109`；`e2e/format/number-precision.spec.ts` |
| 表头选择 | 整行、整列、整表 | `736a2109`；`e2e/selection/header-selection.spec.ts` |
| 清除操作 | 清除内容、清除格式、全部清除 | `094f8ba7`；`e2e/editing/clear-selection.spec.ts` |
| 基础剪贴板 | 复制、同表剪切、矩形粘贴 | `42dd8505`；`e2e/editing/system-clipboard.spec.ts` |
| 粘贴选项 | 仅粘贴值、仅粘贴格式、选区平铺 | `42dd8505`；`e2e/editing/paste-special.spec.ts` |
| 粘贴变换 | 转置、跳过空白、值及格式 | 已验证，未提交；`e2e/editing/paste-options.spec.ts` |
| 编辑输入 | 原始数值、输入法组合键保护、Alt+Enter 换行 | 已验证，未提交；`e2e/editing/input-semantics.spec.ts` |

清除批次验证（2026-09-08）：UI Core 119 文件 / 2144 测试、React 33 文件 / 57 测试通过；
修正 Rust 默认样式写法后相关 15 条核心测试通过，完整真实 Worker E2E 32/32 通过。
类型、构建、lint、循环依赖、展现层边界和文档链接检查通过。复用 P3、G9 与第 2 行等
已有 Rust 种子样式作为清除样例。此次没有修改 Rust 数据模型或添加浏览器工作簿副本。

剪贴板批次验证（2026-09-08）：Rust Core 1587 通过 / 3 条既有忽略项（新增 12 条）；
UI Core 121 文件 / 2156 测试、React 34 文件 / 61 测试、真实 Worker E2E 39/39 通过。
类型、构建、lint、循环依赖、展现层边界检查通过；lite/full WASM 均重建成功。
E2E 复用 A2 粗体、G2 公式、E2 红字种子，覆盖冻结复制、公式平移、剪切引用跟随、外部 TSV、
越界不删除源格、屏幕外整列复制，并保存桌面/窄屏错误提示截图。

链路：浏览器剪贴板事件 → `runSystemClipboardAtom` → 一次 Rust 命令 → 同修订版投影。
Rust 快照负责保留复制时的数据，TS 只传文本与快照 token；没有 JS 工作簿副本。
普通新增文件均 ≤300 行。`auto_fill.rs` 为存量超限文件（2684 行），本批只把原有引用平移
算法抽到 `shift/copy.rs` 供两种操作共用，未扩大为整个自动填充模块重构。

粘贴选项批次验证（2026-09-08）：Rust Core 1598 通过 / 3 条既有忽略项；
UI Core 121 文件 / 2161 测试、React 34 文件 / 63 测试、真实 Worker E2E 47/47 通过。
类型、构建、lint、循环依赖、展现层边界检查通过。复用原有剪贴板命令，只增加模式与目标选区；
Rust 统一检查目标尺寸、边界及保护范围，不在 JS 逐格写入。
测试覆盖原始值与公式结果快照、格式粘贴保留公式、逐块公式平移、尺寸不匹配整次拒绝、
剪切拒绝特殊粘贴后仍能正常移动，并生成桌面与窄屏截图。

这些批次不代表原编号 10–11 完成：跨表剪切、部分公式范围移动会明确拒绝；
多区域、Copy As、仅公式、运算、批注及列宽等剩余粘贴选项继续保留在范围中。

粘贴变换沿用原命令与原 Rust 事务：转置改变源格映射，跳过空白只检查/写入非空源格对应的目标，
值及格式保留原始结果和有效样式。`0`、`false`、空字符串、返回空字符串的公式均不跳过；
转置后的尺寸、每块公式偏移、保护和 spill 检查均由 Rust 决定。没有新增生产模块或 atom 状态。
交互语义参考 [Microsoft 粘贴选项](https://support.microsoft.com/en-us/excel/copy-and-paste-specific-cell-content-in-excel-for-mac)
与 [转置说明](https://support.microsoft.com/en-us/excel/transpose-rotate-data-from-rows-to-columns-or-vice-versa)，
不据此宣称实现了完整 Excel 剪贴板兼容性。

粘贴变换批次验证（2026-09-08）：Rust Core 1610 通过 / 3 条既有忽略项，
UI Core 121 文件 / 2170 测试、React 34 文件 / 66 测试、真实 Worker E2E 55/55 通过。
lite/full WASM、UI Core 构建、React 构建、类型、lint、循环依赖、展现层边界和文档链接检查通过。
新增 8 条 E2E，复用 A2/B2、P3、G9 原有种子，覆盖转置平铺、公式偏移、跳过空白、值及格式、
外部文本与拒绝剪切后的恢复；共用 `e2e/support/clipboard.ts` 消除三个用例文件的重复操作。
桌面及 390px 窄屏截图已检查；窄屏工具栏标签改为随栏滚动，避免遮住下拉菜单，E2E 验证命中点未被遮挡。

粘贴变换批次发现的编辑来源问题已在编辑输入批次修正：Rust 在原投影内返回 `inputText`，
数字用可往返的完整精度文本，公式用原公式；它与 `displayValue` 分开，格式投影与克隆均保留。
React 公式栏与格内编辑都读取该文本，没有增加编辑前 RPC。演示金额按分生成，P3 原值为 125.02。

两个输入框改为 textarea；Alt+Enter 只调用 `insertEditingLineBreakAtom` 替换草稿选中文字，
React 在本次同步渲染后恢复 DOM 光标，Enter 仍复用原提交命令。浏览器组合事件及 keyCode 229
不拦截、不提交、不取消；这些自动化测试不等同于对真实操作系统输入法候选窗的人工验收。
I3 新增两行 Rust 导入种子。显式换行按字号和行数只撑高所在行的 rowStyle，不扫描其他格，
也不在 cellStyle 放高度。按列宽折行的完整 AutoFit 和自动缩高仍未完成。
`keyboard/index.ts` 为存量 374 行文件，本次只添加 Alt+Enter 分支，未扩大为键盘模块重构。

编辑输入批次验证（2026-09-08）：Rust Core 1613 通过 / 3 条既有忽略项；新增多行文本测试 3 条，
WASM 原始编辑数字往返专项 1 条通过；UI Core 122 文件 / 2176 测试，React 35 文件 / 69 测试通过。
真实 Worker E2E 63/63 通过，含新增 8 条编辑输入用例；lite/full WASM、构建、类型、lint、
循环依赖、展现层边界与文档检查通过。桌面与 390px 窄屏截图验证双行编辑框和公式栏。

## 接下来

优先闭合日常编辑：剪贴板剩余语义与完整类型输入 → Rust 撤销重做 → 工作表管理 → 行列结构。
之后继续推进尺寸、合并、冻结、公式、数据管理等剩余项。每批验证完成后更新本文件，
不把“已有 atom”当成产品功能完成，也不因单批完成而结束整个目标。
