# 冻结窗格批次（进行中）

三项功能：冻结首行、冻结首列、按当前单元格冻结／取消冻结。整批尚未验收。

## 当前完成的原生链路

`sheet.freeze` → WASM → Rust Sheet 的 `{ rows, cols }` → 同一次响应里的可见投影。

- 配置只存两个整数；冻结不改值、格式或公式，也不为冻结范围物化单元格。
- 原生历史只保留前后配置；无操作或无效输入不清 redo，不增加修订版。
- 在冻结带内部插入会扩大带，删除会缩小带；恰好在边界插入不改变冻结数。
  结构撤销/重做恢复原边界，移动工作表或删除后撤销仍跟随原工作表身份。
- 可选 `freeze` 快照字段保留配置；旧快照缺省为零。无效、重复或缺失表的配置拒绝整本恢复，
  原工作簿、历史不变。页面保存/打开入口仍未实现，不把绑定层快照当作 UI 持久化完成。
- 新增 `set_frozen_panes`、`frozen_panes` 两个 WASM API，同步更新增量签名快照并通过检查。
- Worker 在写入前校验 JS 整数、当前画布及投影所属表；Rust 操作失败时不发布新投影。
- 传输入口原有 296 行；按单文件规则将新增／重命名处理移至 `sheet-edit.ts`，入口现为 283 行。
  该文件只是已有协议处理，不是 backend、adapter 或另一个运行时。

## 当前验证

- Rust 冻结专项 5 条通过，覆盖历史、稀疏性、边界及工作表身份。
- Rust 核心单测 1631 通过 / 3 条既有忽略；冻结、合并历史、结构和剪切组合专项 22/22。
- WASM 单测 97 通过 / 1 条既有忽略；其中新增冻结绑定与持久化 4 条。lite/full 已重建。
- Worker／投影专项 13 条通过；UI Core＋React 全量 Vitest 195 文件 / 2532 条通过。
- 真实 Worker 冻结协议、已有工作表管理／结构及合并种子 E2E 20/20 通过。
- 最终全量真实 E2E 217/217 通过；新增的是协议测试，尚无冻结区域 UI 验收。
- 类型、lint、两包构建、展现层边界及循环依赖通过。本轮尚未提交、未 push。

## 接下来必须完成

1. UI Core 的冻结 command atom；配置读取只从原生投影派生，不走旧 `viewport/freeze.ts` 的本地历史／可选持久化路径。
2. 同一请求读取冻结区域及滚动区域的数据；四个区域复用现有单元格渲染，不为每个区域创建工作簿或数据 atom。
   冻结范围大于屏幕时仍按实际可见范围读取，不能把整个冻结前缀一次渲染出来。
3. 顶部／左侧固定区域、表头、选区及编辑框共用一致的几何；补齐跨冻结线的拖选、键盘定位和自动滚动。
4. 验证隐藏行列、非等宽列、大字体、合并格跨冻结线、窗口外锚点及切表／历史交叉行为。
5. 接入冻结菜单和原始 Rust 种子；补 React 单测、真实 UI E2E、桌面／390px 截图及全量门禁。

协议测试只证明两个计数和历史，不证明页面已冻结。当前菜单和固定区域显示都尚未接入。
