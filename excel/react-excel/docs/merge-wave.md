# 合并批次（三项功能已验收）

三项用户功能：合并单元格、合并后居中、取消合并。不包含跨行分别合并。
原生基础已提交 `1df3759d`；传输及界面接入已提交 `0b5df39f`，不代表整批验收。
持久化、隐藏锚点及剪贴板阶段已提交 `cb99acfc`；跨表定位修正、导航回归及种子已随 `9164cd97` 提交。

## 已接入的原生链路

`range.merge` → `WasmWorkbook.merge_cells` → Rust 合并及历史 → 同次 Worker 响应中的投影。

- 合并矩形只存于 Rust Sheet，不写进 cellStyle，不创建覆盖格 atom。
- 左上角保留原始值或公式；其它非空内容必须明确确认才删除。
- 合并后居中与清除其它内容共用一条原生历史。
- 取消合并不恢复被清掉的数据，撤销合并才恢复；取消合并历史只存几何，不复制原始值或格式。
- 合并范围参与行列插删与结构撤销，数组公式不能跨越合并格。
- 取消合并后重新投影受阻数组；撤销取消合并会重新阻挡数组并清除其旧投影子格。
- `set_cell_input` 拒绝覆盖格输入，避免生成不可见数据；锚点输入仍使用现有编辑链路。
- Worker 不维护合并副本；一次动作返回原生历史与可见投影，确认拒绝或无操作不增加版本。
- 全表合并范围用于屏幕外导航；只读取与视口相交的锚点内容及格式，不枚举整个合并区。
- 窗口外锚点单独放在 `mergeAnchors`，不放宽普通 `cells` 的窗口边界约束。
- `initialize-workbook.ts` 承接工作簿初始化，Worker 主入口保持 ≤300 行。

## 前一阶段已验证（不代表当前界面接入后的全量结果）

- 合并原生内容/历史 11 条、合并结构位移 4 条通过。
- WASM 新增绑定 2 条通过，全量 WASM 单测 87 通过 / 1 条既有忽略。
- UI Core 合并 Worker/投影 Vitest 13 条通过；原有结构 Worker 4 条回归通过。
- lite/full WASM 已重建，API 快照只增加两个方法，签名检查通过。
- `e2e/workbook/merge-worker.spec.ts` 真实 Vite Worker/WASM 协议测试 2/2 通过。
  覆盖确认、原生居中、屏幕外锚点、覆盖格输入拒绝、取消合并、连续撤销重做。
- UI Core/React 全量 188 文件 / 2479 条通过；现有界面及新增 Worker 协议 E2E 全量 193/193 通过。
- Rust 核心单测 1631 通过 / 3 条既有忽略；类型、生产构建、循环依赖、lint、展现层与文档检查通过。
- 新增空值数组子格用例复现合并漏检，改为复用排序的数组矩形预检。
  `sort.rs` 为 1161 行存量文件，本次仅共享该预检方法，不顺带重构排序模块。
- 最后修复后合并/位移 15 条、排序相关 50 条回归通过；最终 lite/full 重建、合并/结构 E2E 10/10、生产构建通过。

## 当前界面接入与提交前检查

- 已接合并、合并后居中、取消合并菜单与内容丢失确认框。
- 合并格使用 Rust 投影的矩形与锚点渲染；选区、基础方向键/Tab/Enter、公式栏与编辑器定位锚点。
- 本次合并专项真实 E2E 7/7 通过：2 条 Worker 协议、5 条 UI（含桌面及窄屏）。
  已检查 1280/390 宽度的确认框及合并结果截图：文字可读、取消优先聚焦、合并格无内部网格线。
- Rust 新增剪贴板边界保护：拒绝剪切合并区及向覆盖格粘贴内容；这不是完整的合并剪贴板语义。
- 首次提交检查发现 5 条失败：投影刷新重建了选区快照，引用比较误判为用户换了选区。
  表头选择、隐藏行列、剪贴板改为复用既有选区变更标记，保留“移走再移回”失效语义。
- 修复后全量 Vitest 2491/2491 通过；随后补充合并菜单 React 单测 8/8 通过。
- 6 处 lint 超长行已修复；Rust 合并历史与剪贴板边界测试 13/13 通过。
- 本次类型检查、展现层边界、循环依赖与 `git diff --check` 通过。
- 本次全量真实 E2E 198/198、React 生产构建通过。
- 用户要求先提交，因此保存已接入阶段；剪贴板、持久化及 seed 未完成，整批仍未验收。

## 提交后的收尾

- 原生持久化 v1 新增可选 `merges` 字段；旧快照缺省为空，空白大矩形不生成密集单元格。
- Rust Sheet 校验越界、倒序、重叠及覆盖格原始内容；数组投影子格不误当成用户数据。
- 整本恢复先在新工作簿完成，成功后才替换；失败保留原工作簿、历史及剪贴板。
  成功恢复清空旧历史及剪贴板，保留自定义公式注册表；普通公式仍按原机制惰性求值。
- `wasm_workbook_restore.rs` 只负责整本恢复，原文件保留稀疏单元格快照操作；两者均小于 300 行。
- 新增原生持久化 6 条测试；WASM 全量 93 通过 / 1 条既有忽略，Rust 核心 1631 通过 / 3 条既有忽略。
- lite/full 已重建，新增两个真实浏览器 JSON 持久化测试通过；WASM API 签名未改变。
  这是绑定层验收，页面的保存/打开入口仍属于后续功能，不能据此声称已有 UI 持久化。
- 隐藏锚点所在行/列后，合并格只要还有可见部分仍可双击编辑；原失败单测及真实 E2E 已修复。
  可见尺寸复用 `getAxisSpanSize`，不增加 React 状态。
- 新增合并格渲染 React 单测 3 条：屏幕外锚点、隐藏后实际尺寸、工作表隔离。
- 当前全量 Vitest 192 文件 / 2504 条通过；lint、类型、循环依赖、展现层、文档及生产构建通过。
- 最终全量真实 E2E 201/201 通过（包含两种 WASM 持久化及隐藏锚点编辑）；API 签名检查通过。

## 收尾验收（2026-09-09）

- Rust 剪贴板快照携带合并矩形；复制、同表剪切、转置及平铺同时处理内容与几何，撤销/重做仍是一条历史。
- 外部单值粘入已合并格只写锚点；值/公式/数字格式模式保留目标布局。
  仅格式或跳过空白不能借合并隐藏已有内容；不完整覆盖合并区、数组结果或 Table 会拒绝并显示原因。
- 跨表复制页面原本偶发失败，15 次重复出现 1 次。稳定 React 测试证明：名称框从保留的旧投影取表 ID，
  导致切表后输入 C5 又把选区写回旧表。删除这项覆盖，复用 command 的当前选区；未新增状态或传输层。
  Enter、失焦两种提交回归均通过；合并剪贴板 6 个场景连续 5 轮共 30/30 通过。
- 新增真实导航 E2E 6 条：编辑后 Enter/Shift+Enter/Tab/Shift+Tab、四向 Shift 扩选与收缩、相邻合并格双向移动。
- Summary 原始种子新增 A15:D15 居中标题、A17:B18 跨行文本；矩形通过既有初始化写进 Rust，
  覆盖格不存内容，启动历史为空。真实页面验证编辑、取消合并及撤销均通过。
- Rust 批量导入新增整批覆盖格预检，避免初始化绕过保护产生隐藏内容；lite/full 真实 WASM 各一条测试证明
  错误批次不改锚点或工作簿快照。原生持久化测试继续保留，不据此声称页面已有保存/打开入口。
- 全量 Vitest：194 文件 / 2519 条通过。Rust 核心单测：1631 通过 / 3 条既有忽略；
  剪贴板、合并历史与结构专项：54/54；WASM 单测：93 通过 / 1 条既有忽略。
- lite/full WASM 已重建；真实页面全量 E2E 216/216 通过。lint、类型、两包构建、循环依赖、展现层与文档校验通过。
- 按浏览器 QA skill 检查 1280px 合并结果、390px 确认框及剪贴板错误截图：仅外框，文字可读，无内部网格线。
  共享 visual-runtime 缺失，使用仓库 Playwright 的持久化截图与焦点/溢出断言；未额外引入视觉工具链。

## 完整计划仍保留的范围

本批完成合并、合并后居中、取消合并三项，不等于整个 Excel 计划完成。
冻结区交叉随冻结批次验证；完整共享边框语义仍属于格式投影项。
跨表剪切、多区域及其它尚未接入的剪贴板选项继续保留在原编号 10–11，不以本批回归代替。
三项验收不代表原编号 04 的所有扩展场景或整个 41 项计划完成。
