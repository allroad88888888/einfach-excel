# 行列插删批次（进行中）

本批仅做插入行、插入列、删除所选行列，范围不因底层通过而改成“已完成”。
基线提交 `aa102ded`；其后的预检、历史、WASM 入口尚未提交。

## 已完成的原生链路

- `Workbook::try_structural_edit` 统一四种位移的几何及尾部溢出预检。
- 同表/跨表引用随数据移动；删除保留公式外层、IFERROR 及范围幸存部分。
- 冷公式不因随动整体变热；修复越界引用、带空白 spill 后缀的冷/热差异。
- `WorkbookHistory::edit_structure` 把一次插删记入既有 50 条 / 32 MiB 历史栈。
- 保存目标表被移动的稀疏带、带外被改写的公式、目标表尺寸/格式/可见性/筛选/Table 元数据。
- 不保存计算结果或 spill 子格；不复制无关工作表及未移动的前缀大文本。
- 恢复先检查工作簿/工作表身份和 Table 名称冲突，再还原原始存储，最后投影数组并恢复通知。
- 还原同时更新稀疏范围成员通知，避免 `SUM(A:A)` 继续返回恢复前的缓存值。
- `WasmWorkbook.edit_structure(sheet, action, at, count)` 直接调用原生历史；成功后清除旧剪贴板。
- `history_state` 增加可选 `structuralEdit: { action, at, count }`，只供宿主调整视图，不包含历史数据。
- 普通单格、工作表管理、隐藏操作的历史仍走同一栈；不新增 JS 撤销器。

按职责抽出了历史条目的回放分派，`workbook_history.rs` 249 行；普通新增文件均不超过 300 行。
存量 `tests/lazy_structural_retarget.rs` 410 行，只修改一条过期的“删除引用即丢弃公式”断言。

## 已有验证

- 原生结构相关原有八套专项 89 条通过；新增结构历史两套 17 条通过。
- 工作表历史 10 条、行列可见性 7 条回归通过；Core 全量单测 1631 通过 / 3 条既有忽略。
- WASM 单测 85 通过 / 1 条既有忽略，新增插删历史绑定及剪贴板失效 2 条。
- WASM API 增量快照检查通过，lite/full 产物均已重建。
- 现有编辑历史、工作表历史、隐藏功能真实 Worker E2E 26/26 回归通过；文档、展现层边界及差异检查通过。
- 历史测试覆盖四轴反复回放、跨表结果/源码、尺寸/样式/隐藏、筛选快照、删除 Table 恢复、spill、
  无效停泊源码、失败不消耗 redo、跨工作簿拒绝、Table 冲突后重试、混合历史及 atom 数量稳定。
- 内存用例放置两份无关 33 MiB 文本，仍可保留并撤销本次小范围结构历史。

## 尚未接入（下一步）

1. UI Core 的单次 Worker 结构命令及 command atom，包括编辑/保护/忙碌状态的现有约束。
2. 画布数量增减、选区/可见窗口重定位；不能只移动屏幕中的单元格。
3. 原生结果一次返回投影、完整受影响轴的稀疏尺寸、隐藏元数据和历史目录。
4. 结构 undo/redo 用 `structuralEdit` 更新宿主画布；不能只刷新历史 `range` 内的尺寸，位移尾部也变了。
5. Worker 同步标记剪贴板 token 失效，避免 Rust 已清除快照但 JS 仍把旧 token 当成可剪切。
6. React 菜单、UI Core/React Vitest、真实 Worker 插删及撤销 E2E，完整批次验收。

目前没有 React 插删入口或本批插删 E2E；不得拿原生单测或既有界面回归宣称三项用户功能完成。
