# 状态栏统计批次

两批六项随本次提交。以下先记录第一批验收；第二批验证与待办见文末，不代表全部验收通过。

## 使用与数据规则

- 选中任意范围，底部显示 Numerical count / Sum / Average；原先的几何格数改名 Selected，避免混淆。
- Summary `A93:F93` 包含 10、0、-4、公式 `=A93*2`、文本 `30`、布尔 true；初始结果为 4、26、6.5。
  把 A93 改为 20 后变为 4、56、14；撤销、重做跟随 Rust 原生版本刷新。
- 统计完整选区，不限可见窗口或查询页；重叠区域只计一次，数值公式、溢出格与隐藏格都参与。
  文本数字、布尔、错误、非有限数不参与。合并区域只保留原生锚点数据，不按覆盖格重复计数。
- 没有数值时计数为 0，其余为 —；求和超出 f64 范围显示 Out of range，不假造零。
  有限平均值不因求和溢出丢失。结果仍是浮点统计，不宣称十进制任意精度。
- 原生最多接受 1024 个合法选区；超限或非法坐标整次报错，不返回前一部分统计。
  当前 React 仍是连续选区 UI，本批不顺带增加非连续多区选择入口。

## 最短调用链

选区／数据版本变化 → UI Core `selectionAggregatesAtom` → `selection.aggregate` → Rust → 统计结果 → React。

- Rust `workbook_aggregate.rs`：读取原生稀疏数值并计算结果；WASM 文件只转换参数、空值与结果。
- UI Core `selection-aggregates.ts`：只读派生查询，过时 Promise 由 atom 处理，没有 ticket、控制器或新数据副本。
  单独派生数据 revision，使同一版本的滚动不触发整列重查。没有连接时同步返回 idle，不制造无用等待。
- React `SelectionStatus.tsx`：只展示、格式化结果；Suspense 只包统计，不包网格。无需额外 useEffect 或 command 包装只读查询。
- 旧 TS 可见格算术、投影快照、截断预算、未接入配置及旧契约测试已删除，可从 Git 恢复。
  新测试覆盖原生数值与异步派生，不为暂停的 Solid 保留兼容代码。
- Worker runtime 触及文件上限，错误序列化移入已有 `transport.ts`，与响应端错误转换放在一起；Worker 仍只有三个源文件。

## 验证

- Rust 统计专项 7/7；Core lib 1631 通过／3 条既有忽略。覆盖完整稀疏列、重叠、跨表公式更新、空格、溢出、非法输入、数组与隐藏行。
- WASM lib 102 通过／1 条既有忽略；lite/full 已重建，公开签名快照显式增加一个只读方法。
- UI Core 与 React 合计 207 文件／2573 条 Vitest 通过；旧投影契约测试被原生链路测试替换，不是跳过失败项。
- 新 UI E2E 4/4，独立真实 Worker E2E 1/1；全量 E2E 268/268 通过（4.3 分钟）。
- 1280×800、390×800 截图已检查，窄屏标签与统计分行，统计文字未裁切、未盖住网格。
  持久截图入口：`e2e/selection/selection-status.spec.ts`；真实传输入口：`e2e/selection/aggregate-worker.spec.ts`。
- 构建、类型、lint、展现层边界、循环依赖已通过；所有新增／大改普通源文件 ≤300 行。

本批不表示完整产品完成；统计项自定义、更多摘要项等继续按原计划核对。

## 第二批：非空计数、最小值、最大值

- 复用同一次 Rust 稀疏扫描与现有查询；没有新增请求链或 React 状态。
- 非空计数包含文本、布尔、错误及空字符串，真正空白不计；最小值、最大值只看有限数值。
- Summary `A93:F93` 新三项为 6、-4、20；`A94:C94` 含空字符串、返回空字符串的公式、错误，结果为 3、—、—。
- Rust 专项 10/10；WASM lib 102 通过／1 条既有忽略，公开 API 快照通过，lite/full 已重建。
- UI Core 与 React 共 207 文件／2582 条 Vitest 通过；专项真实 Worker/UI E2E 6/6 通过。
- 构建、类型、lint、展现层边界、循环依赖、文档链接检查通过。
- 提交时全量 E2E 已出现失败，不能沿用第一批 268/268 宣称当前全绿；具体失败待定位。
- 截图发现 1280px 下六项统计挤压 Summary 工作表标签；响应式分行及标签完整可见断言待补。
- 按用户要求先提交当前状态，本次不继续修改功能或布局；统计项配置仍未实现。
