import { atom } from '@einfach/core'
import type { FilterSortControllerPort, FilterSortDraftPatch, FilterSortEntrypoint, FilterSortState, UpdateFilterSortAvailableValuesInput, UpdateFilterSortDraftInput } from './types'
import { FILTER_SORT_CAPABILITY_ERROR, FILTER_SORT_TARGET_ERROR } from './constants'
import { CLOSED_FILTER_DROPDOWN_STATE, EMPTY_FILTER_SORT_STATE, closedFilterSortDraft, draftFromState, errorMessage, lifecycleFor, nextFilterSortRequestId, nextFilterSortSessionId, openFilterDropdownState, snapshotFilterSortDraft, sortFilterValues, sameValues, stateStoreWith, stateStoreWithout, entrypointStateFor } from './value-domain'
import { resolveFilterSortEntrypointTarget } from './authority-domain'
import { activeFilterSortEntrypointAtom, activeFilterSortMutationAtom, filterDropdownBackingAtom, filterSortCapabilityBackingAtom, filterSortDraftBackingAtom, filterSortEntrypointStateBackingAtom, filterSortErrorBackingAtom, filterSortLifecycleBackingAtom, filterSortSessionIdBackingAtom, filterSortStateBackingAtom, filterSortSyncTicketBackingAtom } from './state'
import { filterDropdownAtom, filterSortCanCloseAtom, filterSortCapabilityAtom, filterSortDraftAtom, filterSortErrorAtom, filterSortLifecycleAtom, filterSortSessionIdAtom, filterSortStateAtom, filterSortSyncTicketAtom } from './projection-atoms'

const hasActiveTicket = (get: Parameters<Parameters<typeof atom>[1]>[0]) => get(activeFilterSortMutationAtom) !== null || get(activeFilterSortEntrypointAtom) !== null
export const issueFilterSortSyncTicketAtom = atom(null, (get, set) => { const next = nextFilterSortRequestId(get(filterSortSyncTicketAtom)); if (next === null) return get(filterSortSyncTicketAtom); set(filterSortSyncTicketBackingAtom, next); return next }); issueFilterSortSyncTicketAtom.debugLabel = 'spreadsheet.filterSort.issueSyncTicket'
export const setFilterSortAtom = atom((get) => get(filterSortStateAtom), (get, set, { sheetId, state }: { sheetId: string; state: FilterSortState }) => set(filterSortStateBackingAtom, stateStoreWith(get(filterSortStateBackingAtom), sheetId, state))); setFilterSortAtom.debugLabel = 'spreadsheet.filterSort.set'
export const reconcileFilterSortRulesFromEngineAtom = atom(null, (get, set, { sheetId, rules }: { sheetId: string; rules: readonly import('./types').ColumnFilterRule[] }) => set(filterSortStateBackingAtom, rules.length === 0 ? stateStoreWithout(get(filterSortStateBackingAtom), sheetId) : stateStoreWith(get(filterSortStateBackingAtom), sheetId, { rules }))); reconcileFilterSortRulesFromEngineAtom.debugLabel = 'spreadsheet.filterSort.reconcileRulesFromEngine'
export const setFilterSortErrorAtom = atom(null, (_get, set, error: unknown) => set(filterSortErrorBackingAtom, error == null ? '' : errorMessage(error))); setFilterSortErrorAtom.debugLabel = 'spreadsheet.filterSort.setError'
export const captureFilterSortCapabilityAtom = atom(null, (get, set, source: FilterSortControllerPort) => { if (hasActiveTicket(get)) return; let available = false; try { available = typeof source?.setFilterSort === 'function' } catch {}; set(filterSortCapabilityBackingAtom, available); const dropdown = get(filterDropdownAtom); if (dropdown.status !== 'open') return; const sessionId = get(filterSortSessionIdAtom); if (!available) { set(filterSortErrorBackingAtom, FILTER_SORT_CAPABILITY_ERROR); set(filterSortLifecycleBackingAtom, lifecycleFor('blocked', sessionId, dropdown.sheetId!, dropdown.colIndex!)); return }; if (get(filterSortLifecycleAtom).status === 'blocked' && get(filterSortErrorAtom) === FILTER_SORT_CAPABILITY_ERROR) { set(filterSortErrorBackingAtom, ''); set(filterSortLifecycleBackingAtom, lifecycleFor('editing', sessionId, dropdown.sheetId!, dropdown.colIndex!)) } }); captureFilterSortCapabilityAtom.debugLabel = 'spreadsheet.filterSort.captureCapability'
export const clearFilterSortAtom = atom((get) => get(filterSortStateAtom), (get, set, sheetId: string) => { if (!get(filterSortCanCloseAtom)) return; const next = stateStoreWithout(get(filterSortStateBackingAtom), sheetId); set(filterSortStateBackingAtom, next); const nextSession = nextFilterSortSessionId(get(filterSortSessionIdAtom)); if (nextSession !== null) set(filterSortSessionIdBackingAtom, nextSession); const session = nextSession ?? get(filterSortSessionIdAtom); set(filterDropdownBackingAtom, CLOSED_FILTER_DROPDOWN_STATE); set(filterSortDraftBackingAtom, closedFilterSortDraft(session)); set(filterSortLifecycleBackingAtom, lifecycleFor('closed', session, null, null)); set(filterSortErrorBackingAtom, '') }); clearFilterSortAtom.debugLabel = 'spreadsheet.filterSort.clear'
const clearColumn = atom((get) => get(filterSortStateAtom), (get, set, input: { sheetId: string; colIndex: number }) => { if (hasActiveTicket(get)) return; const state = get(filterSortStateAtom)[input.sheetId]; if (!state) return; const rules = state.rules.filter((rule) => rule.colIndex !== input.colIndex); if (rules.length === state.rules.length) return; set(filterSortStateBackingAtom, stateStoreWith(get(filterSortStateBackingAtom), input.sheetId, { rules })) })
export const clearColumnFilterSortAtom = clearColumn; clearColumnFilterSortAtom.debugLabel = 'spreadsheet.filterSort.clearColumn'
export const clearColumnFilterRulesAtom = clearColumn; clearColumnFilterRulesAtom.debugLabel = 'spreadsheet.filterSort.clearColumnRules'
export const openFilterDropdownAtom = atom((get) => get(filterDropdownAtom), (get, set, { sheetId, colIndex }: { sheetId: string; colIndex: number }) => { if (hasActiveTicket(get) || !sheetId || !Number.isSafeInteger(colIndex) || colIndex < 0) return; const sessionId = nextFilterSortSessionId(get(filterSortSessionIdAtom)); if (sessionId === null) { set(filterSortErrorBackingAtom, 'Filter and sort session identity space is exhausted.'); return }; const available = get(filterSortCapabilityAtom); const state = get(filterSortStateAtom)[sheetId] ?? EMPTY_FILTER_SORT_STATE; set(filterSortSessionIdBackingAtom, sessionId); set(filterDropdownBackingAtom, openFilterDropdownState(sheetId, colIndex)); set(filterSortDraftBackingAtom, draftFromState(sessionId, sheetId, colIndex, state)); set(filterSortErrorBackingAtom, available ? '' : FILTER_SORT_CAPABILITY_ERROR); set(filterSortLifecycleBackingAtom, lifecycleFor(available ? 'editing' : 'blocked', sessionId, sheetId, colIndex)) }); openFilterDropdownAtom.debugLabel = 'spreadsheet.filterSort.openDropdown'
export const openFilterDropdownFromEntrypointAtom = atom(null, (get, set, input: { readonly source: FilterSortControllerPort; readonly entrypoint: FilterSortEntrypoint }) => { if (hasActiveTicket(get)) return; let available = false; try { available = typeof input.source?.setFilterSort === 'function' } catch {}; set(filterSortCapabilityBackingAtom, available); const target = resolveFilterSortEntrypointTarget(get); if (!available || target === null) { set(filterSortEntrypointStateBackingAtom, entrypointStateFor('blocked', { entrypoint: input.entrypoint, target, attempt: 1, error: available ? FILTER_SORT_TARGET_ERROR : FILTER_SORT_CAPABILITY_ERROR })); return }; set(filterSortEntrypointStateBackingAtom, entrypointStateFor('idle', { entrypoint: input.entrypoint, target })); set(openFilterDropdownAtom, target) }); openFilterDropdownFromEntrypointAtom.debugLabel = 'spreadsheet.filterSort.openDropdownFromEntrypoint'
export const closeFilterDropdownAtom = atom((get) => get(filterDropdownAtom), (get, set) => { if (!get(filterSortCanCloseAtom)) return; const next = nextFilterSortSessionId(get(filterSortSessionIdAtom)); if (next !== null) set(filterSortSessionIdBackingAtom, next); const session = next ?? get(filterSortSessionIdAtom); set(filterDropdownBackingAtom, CLOSED_FILTER_DROPDOWN_STATE); set(filterSortDraftBackingAtom, closedFilterSortDraft(session)); set(filterSortLifecycleBackingAtom, lifecycleFor('closed', session, null, null)); set(filterSortErrorBackingAtom, '') }); closeFilterDropdownAtom.debugLabel = 'spreadsheet.filterSort.closeDropdown'
export const updateFilterSortDraftAtom = atom(null, (get, set, input: UpdateFilterSortDraftInput) => { const draft = get(filterSortDraftAtom); const lifecycle = get(filterSortLifecycleAtom); if (input.sessionId !== draft.sessionId || lifecycle.sessionId !== draft.sessionId || ['closed', 'pending', 'local-acknowledged', 'refreshing', 'refresh-failed', 'outcome-unknown'].includes(lifecycle.status)) return; const patch: FilterSortDraftPatch = { ...input.patch, ...(input.patch.selectedValues === undefined ? {} : { selectedValues: sortFilterValues(input.patch.selectedValues) }) }; set(filterSortDraftBackingAtom, snapshotFilterSortDraft({ ...draft, ...patch })); if (lifecycle.status === 'error') { set(filterSortLifecycleBackingAtom, lifecycleFor('editing', draft.sessionId, draft.sheetId, draft.colIndex)); set(filterSortErrorBackingAtom, '') } }); updateFilterSortDraftAtom.debugLabel = 'spreadsheet.filterSort.updateDraft'
export const updateFilterSortAvailableValuesAtom = atom(null, (get, set, input: UpdateFilterSortAvailableValuesInput) => { const draft = get(filterSortDraftAtom); if (input.sessionId !== draft.sessionId || input.sheetId !== draft.sheetId || input.colIndex !== draft.colIndex) return; const availableValues = sortFilterValues([...draft.availableValues, ...input.values]); const selectedValues = draft.selectionMode === 'all' ? [...availableValues] : [...draft.selectedValues]; if (sameValues(availableValues, draft.availableValues) && sameValues(selectedValues, draft.selectedValues)) return; set(filterSortDraftBackingAtom, snapshotFilterSortDraft({ ...draft, availableValues, selectedValues })) }); updateFilterSortAvailableValuesAtom.debugLabel = 'spreadsheet.filterSort.updateAvailableValues'
